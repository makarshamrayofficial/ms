{
  "name": "ms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build"
  },
  "dependencies": {}
}

npm install --save next react react-dom


{
  "scripts": {
    "next build": "next build && next export"
  }
}